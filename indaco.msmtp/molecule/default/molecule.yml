---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: virtualbox
lint: |
  set -e
  yamllint .
  ansible-lint
platforms:
  - name: instance
    box: ubuntu/noble64
    memory: 512
    cpus: 1
provisioner:
  name: ansible
verifier:
  name: ansible
