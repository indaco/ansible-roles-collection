---
galaxy_info:
  author: indaco
  description: Ansible role to install and configure msmtp as a lightweight SMTP client
  # company:

  # If the issue tracker for your role is not on github, uncomment the
  # having a resolve next line and provide a value
  # issue_tracker_url:

  license: CC-BY-4.0

  min_ansible_version: "2.17"

  # Provide a list of supported platforms, and for each platform a list of versions.
  platforms:
    - name: Raspbian
      versions:
        - all
    - name: Ubuntu
      versions:
        - all

  galaxy_tags: []
    # List tags for your role here, one per line. A tag is a single word
    # or a hyphenated phrase that helps categorize a role. Galaxy uses
    # these tags to find roles. See Ansible Docs for more info on tags.

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the
  # '[]' above if you add dependencies.
